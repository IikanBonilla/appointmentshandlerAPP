# Configuración básica de SQLite
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.url=jdbc:sqlite:mydatabase.db
spring.datasource.username=iikan
spring.datasource.password=05062927

# Dialecto de Hibernate para SQLite
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Si quieres usar base de datos en memoria
# spring.datasource.url=jdbc:sqlite:memory:myDb?cache=shared